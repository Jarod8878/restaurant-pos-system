# restaurant-pos-system
Repository for Point of Sales (POS) web application that uses React, Node.js. Hosted on local server, with mobile phone connected to same IP address as hosting device via API_BASE
